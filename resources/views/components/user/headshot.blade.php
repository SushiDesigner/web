<img src="{{ asset('img/placeholder/icon.png') }}" alt="{{ $user->username }}" title="{{ $user->username }}" @class(['img-fluid rounded-circle shadow-sm user-headshot', $class ?? '']) width="{{ $width }}">
